<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>unitree_go</name>
  <version>0.0.1</version>
  <description>
    Minimal unitree_go interfaces (SportModeState + IMUState + TimeSpec) so the
    VAT bridge can subscribe to /sportmodestate on the Go2. The field layout is
    the subset the client decoder (kinematics.py) reads; trailing firmware fields
    (e.g. path_point) are ignored positionally during CDR deserialization.
  </description>
  <maintainer email="dev@vat.local">VAT</maintainer>
  <license>BSD-3-Clause</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <member_of_group>rosidl_interface_packages</member_of_group>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
