# ============================================================================
# VAT public configuration  —  SAFE TO COMMIT (VPN-internal addresses only)
# ============================================================================
# This file is the single source of public, non-secret config. It is consumed
# two ways and is valid for both:
#   * the Makefile        →  `include vat.env` (the control file exports these)
#   * shell scripts       →  `set -a; . ./vat.env; set +a`
#
# Edit these to match your deployment, commit, and every machine agrees.
# (KEY=value only, no spaces around '='; '#' starts a comment.)

# ── Zenoh router : the hub every process connects to ────────────────────────
# Public VPN IP of the host that runs the router (`make router`).
# IMPORTANT: the mapping server may live in a DIFFERENT datacenter than the
# router, so this must be the router's reachable address — never localhost.
ROUTER_IP=100.76.214.80
# ROUTER_IP=100.87.118.34
ROUTER_PORT=7447

# Endpoint that clients/peers dial (mapping server, robot container, client).
ZENOH_ROUTER=tcp/${ROUTER_IP}:${ROUTER_PORT}
ZENOH_CONNECT=tcp/${ROUTER_IP}:${ROUTER_PORT}
# What the router process itself binds to (on the router host).
ZENOH_LISTEN=tcp/0.0.0.0:${ROUTER_PORT}

# ── Identity / key schema ───────────────────────────────────────────────────
ROBOT_NAME=go2
SERVER_PREFIX=server/prism
# DDS interface the container's bridge binds to reach the Go2 MCU (which
# publishes /sportmodestate). Leave empty to AUTO-DETECT (prefers the NIC on
# the Unitree subnet 192.168.123.x); set explicitly if auto-detect picks wrong.
# Find it on the robot with:  ip -br addr
NET_IFACE=
# unitree_ros2 git ref (branch/tag) for the bridge's message types, built into
# the container image. If /sportmodestate shows [no data] after a rebuild, your
# firmware's message layout differs from this version — check the host with
#   ros2 interface show unitree_go/msg/SportModeState
# and pin the matching unitree_ros2 branch/tag here, then rebuild.
UNITREE_ROS2_REF=master

# ── PRISM sliding window (mapping server) ───────────────────────────────────
# Window of TRANSMITTED frames (i.e. THROTTLE_FPS rate, NOT CAMERA_FPS) that
# PanoVGGT infers per submap. OVERLAP frames are shared with the previous window
# (chaining/anchoring). 12/4 ≈ 2.7 s of footage at THROTTLE_FPS=3 → lower latency
# and more frequent submaps than 16/4 (slightly less per-window context).
# OFFLINE-PARITY TEST: matched to gradio (16/4). Prior live value: WINDOW_SIZE=12.
# LIVE (favor latency): 9/3 → stride 6 → smaller windows, more frequent submaps.
# LOWER-LATENCY LIVE: 6/3 → stride 3 → a submap fires every 3 new frames instead of
# 6, i.e. ~1.2 s of buffering at THROTTLE_FPS=2.5 instead of ~2.4 s. This roughly
# halves the frame-accumulation part of the VGGT correction latency (the ~4 s corr_lag
# seen in the fuser log), so each pose fix is fresher and SMALLER → smaller avatar
# corrections. Cost: 6-frame windows give PanoVGGT less temporal context than 9, so
# per-window scale/geometry can be slightly noisier, and the submap cadence ~doubles
# (server was ~50% busy at 9/3, so there is headroom — but watch BACKLOG_MAX_FRAMES:
# if the backlog guard starts firing, the GPU can't keep the faster cadence, so raise
# back toward 9/3). Revert to 9 if the map quality regresses.
WINDOW_SIZE=6
OVERLAP=3
# TSDF voxel size (m). 0.03 (3 cm) ≈ half the points of 2 cm → smaller clouds,
# lower latency/bandwidth. Drop to 0.02 for finer geometry at higher cost.
# OFFLINE-PARITY TEST: matched to gradio (0.02). Prior live value: VOXEL_SIZE=0.025.
VOXEL_SIZE=0.03
# Streamed cloud density (m), decoupled from the TSDF voxel above. Coarser = less
# bandwidth; the mapper stays fine for internal quality. Set = VOXEL_SIZE to disable.
# KEEP THIS DECOUPLED FROM VOXEL_SIZE. The cube push channel is CongestionControl.DROP,
# so if a submap's changed-cube bundle is too big it is dropped wholesale and the
# viewer shows nothing (cloud→client = 0 KB/s). With decay OFF the map grows unbounded,
# which makes a 2 cm stream balloon past what the push will carry → blank viewer.
# 0.05 is known-good; lower toward 0.03 ONLY while watching cloud→client stay non-zero.
# The internal TSDF + on-demand full-res snapshot stay at VOXEL_SIZE (gradio quality).
# 0.08 (was 0.06): fewer streamed points → smaller/faster client decode+GPU upload per
# update → shorter render hitches, which is what makes the pose display stutter (the
# client is single-process/GIL-bound, so heavy cloud work stalls the render+pose too).
# This is the pose-smoothness ↔ cloud-detail dial; lower it toward 0.05 if you want denser
# clouds and the render stays smooth.
STREAM_VOXEL_M=0.065

# ── Mapping mode: reset / hybrid + streaming transport ──────────────────────
# These were previously left to mapping_config.py defaults, but vat.env is exported to
# BOTH server and client, so they MUST be set here to actually take effect (otherwise
# the running config silently differs from intent — the cause of the "still whole-map
# snapshot / blank viewer" symptom).
#
# STREAM_MODE=blocks: stream only the cubes that changed each submap (+removed keys),
# Draco-compressed — tiny per-update payloads, so the realtime pose stream isn't starved
# by a big whole-map burst. "snapshot" = whole cloud each submap (simplest; big bursts).
STREAM_MODE=blocks
# Reset-based mapping (clean, no cross-batch drift). 0 = deprecated online-accumulate.
PRISM_RESET_EACH_BATCH=1
# HYBRID: full rebuild every N batches; the N-1 in between EXTEND online + stream small
# block deltas. 1 = reset every batch (cleanest). 3 = ~2 cheap delta updates per rebuild.
RESET_PERIOD_SUBMAPS=3
# Frames rebuilt per reset (= WINDOW_SIZE + k*stride). MUST track WINDOW_SIZE/OVERLAP
# or the reset batch tiles into too many windows: at 6/3 (stride 3) the old 15 =
# 6 + 3*3 → 5 windows/submaps per batch, which (a) ballooned the submaps-before-clear
# and (b) anchored the batch's EARLY per-submap corrections to keyframes from the START
# of the ~6 s span → that was the ~4 s corr_lag. 9 = 6 + 1*3 → 2 windows, matching the
# old 9/3 batch shape: fewer submaps per clear AND the oldest per-submap correction is
# ~1.5 s instead of ~4 s. Raise to 12 (3 windows) if you want more area per rebuild.
RESET_WINDOW_FRAMES=9
# Soft reset: wipe the nvblox volume in place (mapper.clear()) instead of reallocating.
SOFT_RESET=1
# Reset mode: extract+colour the mesh once on the last window (integrate-only before).
RESET_EXTRACT_LAST_ONLY=1
# Publish the world-frame ESDF slice for navigation each submap.
COMPUTE_ESDF=1
# Stream decimation mode: none | voxel_centroid | voxel_center | stride
STREAM_DECIMATE_MODE=voxel_centroid
STREAM_STRIDE=3
# Backlog guard: resync if the server falls this far behind real time (processing
# slower than capture → growing latency). This is now a LAST RESORT — gentle decay
# (above) + MESH_EXTRACT_EVERY (below) bound per-submap cost so it should rarely fire.
# When it does fire it rebuilds the live (nav) TSDF clean, but the viewer keeps the
# last surface as a display-only base (RESYNC_PRESERVE_DISPLAY) so it never goes blank.
# 0 disables the guard entirely (latency can then run away — only for parity testing).
BACKLOG_MAX_FRAMES=45
BACKLOG_KEEP_FRAMES=36
# Backlog-resync display continuity: when the backlog guard fires, keep the last
# colored surface as a DISPLAY-ONLY base in the streamed cloud so the viewer doesn't
# blank out during the clean rebuild. The base does NOT feed the live TSDF/ESDF, so
# no stale geometry leaks into navigation. It ages out after RESYNC_BASE_HOLD_SUBMAPS
# submaps (avoids a lingering seam once the fresh map covers the area). 0 = off.
RESYNC_PRESERVE_DISPLAY=1
RESYNC_BASE_HOLD_SUBMAPS=8
# Streamed-map observation-TTL (this is the "stale voxels never disappear" fix).
# A cube the camera has left behind and NOT re-observed within this many submaps is
# dropped from the streamed grid → it leaves the manifest/push as a `removed` key →
# the client deletes it (no whole-map resend). Revisiting re-observes and re-adds it.
# Tune for how far behind the robot you want to keep geometry: at ~3 Hp / ~0.4 s per
# submap, 40 ≈ keep ~15-25 s of recent map. 0 = off (keep everything ever seen).
MAP_TTL_SUBMAPS=40
# "Observed" = cube centre within this many metres of a submap's camera positions.
# Empty/0 → MAX_DEPTH (you can only refresh what the 360° camera could actually see).
MAP_TTL_RADIUS_M=
# nvblox TSDF prune radius (m) — prunes the TSDF VOLUME itself (not just the stream),
# so the ESDF used for NAVIGATION only sees a bounded, recent local map around the
# robot, and the mesh-pull cost stays constant as you travel (this is the real fix for
# the "latency grows over time" backlog). Bounds the TSDF, so the viewer also shows a
# moving local bubble of this radius. Supersedes TSDF_DECAY when set.
# ►► SET TO 0 FOR SoTA BENCHMARKS (full accumulation, no pruning). ◄◄
# DISABLED (0): this nvblox build's Python bindings do NOT expose a radius-clear
# (clearOutsideRadius lives in private core templates), so the radius path just fell
# back to decay anyway. Carving now runs through TSDF decay above (TSDF_DECAY_FACTOR),
# which is the time/batch sliding window. Re-enable only after binding clearOutsideRadius
# in the nvblox fork (egocentric bubble that also carves currently-visible geometry).
TSDF_PRUNE_RADIUS_M=0
# ── Mapping quality knobs (LIVE config) ─────────────────────────────────────
# These began as an "offline-parity" experiment (match the gradio offline pipeline
# bit-for-bit to prove the quality gap was config-driven) but are now the COMMITTED
# LIVE values. They OVERRIDE the mapping_config.py defaults, so do NOT delete them
# expecting the old behaviour — deleting reverts to the code defaults. Each inline
# comment shows the code default and which way to tune.
#
# nvblox TSDF decay = time-based fade/carve of geometry NOT re-observed (separate
# from the always-on TSDF ray free-space carving). 0 = full accumulated scene like
# gradio (best raw quality, but grows unbounded + keeps stale/drifted ghosts).
# GENTLE DECAY (nav): 1 with DECAY_EVERY_N>1 = a SLOW sliding window that bounds map
# growth, thins drifted ghost-duplicates, and clears stale obstacles, while a 360°
# camera re-observing keeps real static surface refreshed so it doesn't erode.
# DECAY_EVERY_N is the dial: higher = cleaner/more persistent, lower = more
# aggressive safety. 3 ≈ decay every ~3 submaps. Set TSDF_DECAY=0 for offline-parity
# quality benchmarking. (config default: TSDF_DECAY=1, DECAY_EVERY_N=1)
TSDF_DECAY=1
DECAY_EVERY_N=1
# TSDF decay rate — THE carving knob. nvblox multiplies voxel weight by this each
# decay() call and deallocates below threshold; re-observed voxels recover, so it's a
# per-submap sliding window of ~ln(1e-3/W)/ln(factor) submaps. nvblox's default 0.95
# ≈ 180 submaps (looks like "carving doesn't work"); 0.8 ≈ ~30, lower = shorter memory.
TSDF_DECAY_FACTOR=0.8
# Make a fully-decayed voxel read as FREE in the ESDF so navigation treats reclaimed
# space as traversable (not unknown). 1 = on (recommended for nav).
TSDF_DECAY_SET_FREE=1
# Keyframe gating: live skips integrating frames unless the camera moved enough.
# Gradio integrates EVERY frame. 0/0/0 = no gating (parity). (config defaults:
# 0.05 m / 8 deg / 1.0 s)
KEYFRAME_MIN_TRANS_M=0
KEYFRAME_MIN_ROT_DEG=0
KEYFRAME_MAX_INTERVAL_S=0
# Cubemap face resolution. Gradio uses 768; live default 512. Higher = sharper
# geometry. (config default: FACE_SIZE=512)
FACE_SIZE=768
# ── Cloud bandwidth ─────────────────────────────────────────────────────────
# Ceiling-plane clip: drop points above this world-Z height (toy-box top-down
# view + bandwidth). EMPTY = OFF (send the whole cloud, no slicing). Live-settable
# over Zenoh: zenoh put --key 'server/prism/config/ceiling_z' --payload 2.2
# (send 'off' to disable again).
CEILING_Z=
# Draco point-cloud compression (server encodes, client auto-decodes): position
# quantisation bits (10 ≈ 1-2 cm over a room, fine for 3 cm voxels) and level
# (10 = max ratio). Lower bits = smaller; raise for finer maps.
DRACO_QUANT_BITS=10
DRACO_LEVEL=10
# ── Metric-scale stability (fixes layered ground + live-viewer ghosting) ────
# See docs/streaming_poc.md → "Online metric scale".
# Anchor the map's metric scale to the MEDIAN floor scale of the first N confident
# windows instead of locking to a (possibly outlier) first window. 1 = old
# lock-on-first-window behaviour.
SCALE_WARMUP_WINDOWS=3
# Reset-boundary scale RETUNE (slow drift-correction; default OFF). After warm-up the
# metric scale is locked and never revisited. With RESET_RESCALE_ON_FLOOR=1, at each
# RESET rebuild — ONLY if the floor is re-observed with confidence ≥ MIN_CONF — the
# locked scale is nudged toward the fresh floor estimate by GAIN (once per batch); a
# low-confidence floor is ignored and the locked/VGGT scale is kept untouched. Reset-only
# + confidence gate + small gain corrects slow scale drift without the per-submap
# free-Sim3 compounding (map inflation/ghosting). Leave OFF until validated on the rig;
# raise GAIN for faster tracking, raise MIN_CONF to be stricter about when to retune.
RESET_RESCALE_ON_FLOOR=0
RESET_RESCALE_GAIN=0.15
RESET_RESCALE_MIN_CONF=0.6
# Depth integration scale. 0 (default) = integrate all depth at the single
# committed scale, exactly like the offline gradio pipeline (camera height only
# anchors scale on the first scene; PanoVGGT carries it after). 1 = re-scale each
# window's depth to its own floor estimate — pins the floor flat but the ~±4%
# per-window floor noise makes WALLS jitter/ghost. Only enable for a genuine
# monotonic floor climb across very different scenes.
DEPTH_SCALE_FROM_FLOOR=0
# Pose-correction safety (avatar back-propagation). See docs/streaming_poc.md →
# "Pose correction stability".
#  * outlier gate: reject a correction implying travel faster than this (m/s)
#    since the last one, plus a fixed margin (m);
CORRECTION_MAX_SPEED=2.5
CORRECTION_JUMP_MARGIN=0.75
#  * deadband: a correction within this distance/angle of the last published one
#    is treated as VGGT still-scene noise and NOT republished, so a stationary
#    robot's avatar stops twitching on every green correction. Raise if it still
#    jitters when still; lower if the avatar lags small real moves.
CORRECTION_DEADBAND_M=0.06
CORRECTION_DEADBAND_DEG=3.0
# Monotonic guard: the server publishes a correction per SUBMAP (several per batch),
# and a reset batch tiles its rebuilt span oldest-window-first — so across a batch
# boundary the first submap can carry a keyframe slightly OLDER than the previous
# batch's last correction. 1 = drop those backward-in-time re-anchors so the robot
# only ever moves to a fresher pose (keeps max update rate; just skips staler ones).
# 0 = publish every submap's correction regardless of order.
CORRECTION_MONOTONIC=1
# --- VGGT anchor convergence (ESKF pose backend) ------------------------------
# How much of each VGGT fix is folded into the correction TARGET. 1.0 = converge the
# avatar ALL THE WAY to VGGT (no residual "half-way" offset). The ESKF slews the LIVE
# pose to that target over CORRECTION_SLEW_TAU seconds, so full gain is smooth, not a
# teleport. Lower toward 0.5 only to add extra low-pass smoothing of noisy fixes.
CORRECTION_POS_GAIN=1.0
CORRECTION_ROT_GAIN=1.0
# Slew time-constant (s) the live world<-odom anchor eases toward the target over.
# 0.12 converges the avatar to a fix in ~0.4 s (~95% by 3*tau) — smooth but snappy;
# 0.3 felt sluggish. Range ~0.08 (very snappy, slight visible step) .. 0.2 (gentle).
# 0 = snap the full correction instantly (only advisable once corr_lag is small).
CORRECTION_SLEW_TAU=0.12
# Cap points in the STREAMED snapshot (full-res still available via the on-demand
# 'pcd_snapshot' query / make fetch_pcd). Keeps the uplink from saturating.
CLOUD_STREAM_MAX_POINTS=60000

# ── Camera: RICOH Theta X over UVC (robot) ──────────────────────────────────
# (The sharpness window is tuned live over Zenoh, not here, to avoid a name
#  clash with the mapping server's PRISM window — see docs/streaming_poc.md.)
# Transmit rate (frames/s robot→server) = the rate PanoVGGT consumes. The mapping
# server keeps up only if THROTTLE_FPS ≤ (WINDOW_SIZE-OVERLAP)/T_submap; at 8 new
# frames/submap and ~3 s worst-case submaps, the sustainable rate is ~2.5-2.7 fps.
# 3.0 sat just over that cliff → slow backlog growth. 2.5 gives headroom with a
# negligible (0.33→0.40 s) inter-frame motion increase for VGGT. Bounding the map
# (TSDF_PRUNE_RADIUS_M) keeps T_submap from growing so this margin holds over time.
THROTTLE_FPS=2.5
JPEG_QUALITY=70
# Live-frame transmit codec: webp ≈25-35% smaller than JPEG at equal quality → less
# robot WiFi-uplink bandwidth (the uplink the pose stream shares → less pose starvation).
# Server decodes transparently (cv2.imdecode sniffs the format). Set FRAME_CODEC=jpeg to
# revert. WEBP_QUALITY 0-100 (visually ~= JPEG quality).
# Lowered 82→76: ~15-20% smaller frames for less robot-uplink volume (the uplink the
# pose stream shares → less pose starvation), with a negligible quality hit for the
# VGGT depth input. Raise back toward 82 if the map visibly loses detail; lower toward
# 70 for more uplink headroom.
FRAME_CODEC=webp
WEBP_QUALITY=76
CAMERA_FPS=30.0
# TCP send-buffer cap (bytes) on the robot's FRAME uplink only (theta_camera.py).
# Bounds how many frame bytes can queue un-preemptable in the kernel ahead of the
# realtime pose packets on the shared physical uplink → shorter pose latency during
# frame bursts. Empty/0 = OS default (off). 262144 (256 KiB) ≈ a couple of frames;
# lower toward 131072 for more aggressive pose latency, raise if frame throughput
# suffers on a fast link. Does NOT touch the pose/teleop or server→client links.
FRAME_SO_SNDBUF=262144

# Real-time transmit size. The robot captures at THETA_MODE, picks the sharpest
# frame, then DOWNSCALES to this for the live Zenoh stream (PanoVGGT input).
# Full-res twins of the same frames go to the archive below (same seq/ts/height).
TRANSMIT_WIDTH=1036
TRANSMIT_HEIGHT=518

# v4l2 device exposing the Theta UVC stream (fed by `make theta-uvc`). RealSense
# owns /dev/video0..N, so this is /dev/video10 (match VIDEO_NR in theta_uvc.sh).
THETA_DEVICE=/dev/video10
# Frozen-stream watchdog (s): if the captured frame is byte-identical for this
# long the camera/feed is treated as dead — the container stops publishing the
# stale frame and reopens the capture, recovering automatically when you reboot
# the camera and re-run `make theta-uvc` (no robot-docker restart). 0 disables.
STALE_TIMEOUT_S=2.5
# Camera capture/stream resolution: 2K (1920x960) or 4K (3840x1920). 4K gives the
# full-res archive its detail but needs a Jetson that can HW-decode 4K H.264
# (Xavier/Orin OK; Nano cannot). Drop to 2K if `make theta-uvc` stalls at 4K.
THETA_MODE=4K
# Advanced: a full GStreamer pipeline (needs OpenCV built with GStreamer +
# gstthetauvc). If set, it overrides THETA_DEVICE. Leave empty otherwise.
THETA_GST_PIPELINE=

# ── Full-res frame archive (robot) ──────────────────────────────────────────
# Each transmitted frame also gets its FULL-RES (THETA_MODE) twin written to a
# local SQLite-indexed archive, tagged with the SAME seq / timestamp / camera
# height, for offline reconstruction (Gaussian splats, NeRF, …). The server can
# fetch any full-res frame on demand by seq. Oldest frames evicted first (FIFO).
ARCHIVE_ENABLE=true
# Archive path INSIDE the container; ARCHIVE_DIR_HOST is bind-mounted onto it.
ARCHIVE_DIR=/archive
ARCHIVE_DIR_HOST=/data/vat-archive
# Hard cap on total archive size (oldest evicted first). Accepts raw bytes or a
# human suffix: KB/MB/GB/TB (powers of 1024).
ARCHIVE_MAX_BYTES=10GB
# JPEG quality for the full-res archived frames (higher than the transmit copy).
ARCHIVE_JPEG_QUALITY=92

# ── Pose fuser (robot) ──────────────────────────────────────────────────────
# Lowered 50→30: the client renders at 60 fps and DEAD-RECKONS between authoritative
# samples (PosePredictor), so 30 Hz is plenty and halves the pose packet count that has
# to squeeze past the camera-frame bursts on the shared robot WiFi uplink. Raise back to
# 50 only if the avatar visibly lags real motion (and the uplink has headroom).
PUBLISH_HZ=30.0
FALLBACK_BODY_HEIGHT=0.30
# --- Pose estimator backend ---------------------------------------------------
# Which state estimator fuses the inputs into the authoritative pose. ALL are pure
# NumPy now (the gtsam backend was removed -- its aarch64/cp310 wheel never imported
# on the Jetson, so it always silently fell back):
#   eskf   -> DEFAULT. Wheel + IMU error-state Kalman filter with a delayed VGGT
#             re-anchor. Velocity is a FILTERED STATE (propagated by the accelerometer,
#             nudged by the wheels) so it stays continuous through wheel dropouts ->
#             the avatar no longer freezes between corrections. Lateral is a soft
#             constraint so a strafe transient can show instead of being pinned to 0.
#   graph  -> legacy wheel+gyro sliding-window pose graph.
#   blend  -> legacy single-anchor complementary blend.
POSE_BACKEND=eskf
# ESKF tuning (only used when POSE_BACKEND=eskf):
#   ESKF_ACCEL_SIGMA  process accel noise (m/s^2). Higher = trust the wheels more and
#                     let velocity react faster; lower = smoother but laggier.
#   ESKF_WHEEL_SIGMA  forward wheel-velocity measurement noise (m/s). Lower = track the
#                     wheels tighter (after WHEEL_RADIUS is calibrated).
#   ESKF_LAT_SIGMA    lateral (non-holonomic) softness (m/s). Raise to let strafes show
#                     via the IMU; lower to hard-pin lateral to 0 for a pure roller.
#   ESKF_VERT_SIGMA   vertical velocity measurement noise (m/s).
#   ESKF_ZUPT_VX / ESKF_ZUPT_GYRO  "stationary" thresholds for the accel-bias learner.
#   ESKF_BIAS_BETA    how fast the accel bias is learned while stationary (EMA rate).
ESKF_ACCEL_SIGMA=0.5
ESKF_WHEEL_SIGMA=0.05
ESKF_LAT_SIGMA=0.10
ESKF_VERT_SIGMA=0.10
ESKF_ZUPT_VX=0.03
ESKF_ZUPT_GYRO=0.05
ESKF_BIAS_BETA=0.01
# How long (s) after the last VGGT correction the published pose is still labelled
# FIX_CORRECTED (green avatar). The ESKF keeps re-anchoring live odometry to the last
# fix, so the pose stays VGGT-anchored between the discrete corrections -- hold it green
# a little longer than the correction cadence (~2-3 s, see docs/robot_data_sources.md) so
# it does not flicker to amber (dead-reckon) in the gaps. This is display only; it does
# not change the estimate. Lower it to reflect a stalled correction stream sooner.
FIX_HOLD_S=3.0
# Which Go2 sport-state topic the fuser/camera track for body height, tilt and
# limb data. The robot publishes the high-rate `sportmodestate` ONLY while the
# motion service is active, but always publishes a low-frequency
# `lf/sportmodestate` (~10 Hz). Default to the always-on lf copy; set to
# `sportmodestate` for the full-rate stream once the dog is up and moving.
SPORT_TOPIC=lf/sportmodestate
# Dead-reckoning odometry: we compute our own from /lowstate (the Go2-W has no
# robot_odom, and lf/sportmodestate.velocity is zeroed). The fuser fuses the IMU
# attitude with WHEEL odometry — the 4 wheel motors' speed × this rolling radius.
# 7-inch tire ≈ 0.089 m nominal; loaded ≈ 0.085 m. CALIBRATE: drive a known
# distance and scale so the dead-reckon matches; flip the sign if it runs backward.
WHEEL_RADIUS=0.085
# Selfie-stick geometry: camera pose in the base frame, metres. MEASURE THESE.
# Camera is straight up on the middle of the dog's back, 80 cm above the body
# origin. X = fore/aft (+forward, tweak if the mount sits behind centre), Z = up.
STICK_OFFSET_X=0.0
STICK_OFFSET_Y=0.0
STICK_OFFSET_Z=0.80
# Camera→base rotation. Default maps PanoVGGT's optical frame (equirect centre =
# forward) to the REP-103 base. If the avatar's heading is rotated vs the cloud,
# nudge STICK_YAW_TRIM_DEG (e.g. 90 / 180 / -90) until they line up.
STICK_MOUNT_RPY_DEG=-90,0,-90
STICK_YAW_TRIM_DEG=0
# Camera height stamped into each frame (drives PRISM's per-submap METRIC SCALE).
# Must be CONSISTENT or the online map misaligns. 'const' = one fixed height for
# every frame (like the gradio run that aligned perfectly); 'legs' = stance-aware
# height from the leg FK ground plane (stable when the dog crouches/stands).
CAMERA_HEIGHT_MODE=const
CAMERA_HEIGHT_M=1.15      # measured ground→camera (standing)

# ── Client viewer (pose smoothness) ─────────────────────────────────────────
# The viewer is a single Python process (one GIL). The whole-cloud GPU upload
# (set_data) is the expensive per-tick op; at the 60 fps render rate it starves the
# render thread AND the pose callbacks together (pose rate collapses, pose+cloud
# latency spike together, render stalls climb — the "jumpy/bouncing pose" symptom).
# CLOUD_RENDER_HZ coalesces that upload to a lower rate; block deltas accumulate
# losslessly in the client store between uploads, so no geometry is lost. The avatar
# still redraws every render tick. 10 is a good default; raise toward RENDER_HZ only
# if the cloud visibly lags and the pose stays smooth. 0 = upload every tick (old).
CLOUD_RENDER_HZ=10.0
# The PosePredictor buffers poses on the fuser's own per-sample timestamp (source
# timeline) rather than packet-receipt time, so a bursty arrival (caused by the GIL
# stall above) no longer teleports the avatar. Adaptive render delay (seconds): the
# avatar renders this far behind the newest sample so it interpolates (smooth) instead
# of extrapolating (overshoots→snaps). It auto-sizes between MIN and MAX from measured
# transport jitter. Raise MAX if the link is very bursty; lower MIN for less latency.
POSE_ADAPTIVE=1
POSE_RENDER_DELAY_MIN_S=0.0
POSE_RENDER_DELAY_MAX_S=0.15
# Robot avatar clearance: the drawn model is lifted so its WHEELS rest on the floor
# instead of sinking to their hubs (the pose Z is the base above the wheel-hub plane,
# one radius above true ground). Empty = use WHEEL_RADIUS automatically; set a value to
# override, or 0 to disable the lift.
ROBOT_GROUND_CLEARANCE_M=
# Surfels: render the cloud as surface-normal-oriented, UNLIT SQUARE quads that tile into
# a surface, instead of camera-facing point splats. Normals are estimated ON THE CLIENT
# (3×3×3 grid-neighbour PCA, built off the render thread) so NO normals are transmitted —
# zero extra uplink, which matters because the cloud bandwidth is tight. PCD_SURFEL=0
# falls back to the plain point cloud. SIZE_M is both the neighbour-grid pitch for the
# normal fit (set it to STREAM_VOXEL_M) and the square edge length so squares tile.
PCD_SURFEL=1
PCD_SURFEL_SIZE_M=0.065

# ── Teleop (robot drive) ────────────────────────────────────────────────────
# The client streams cmd_vel; the robot's teleop_bridge relays it to the Go2
# sport Move API and STOPS the robot if the stream pauses (deadman). Start
# conservative and keep the physical Unitree remote in hand as the hard e-stop.
TELEOP_RATE_HZ=20.0       # cmd_vel stream + relay rate
TELEOP_TIMEOUT_S=0.3      # deadman: no command for this long → StopMove
TELEOP_MAX_VX=0.3         # forward/back clamp (m/s)
TELEOP_MAX_VY=0.2         # strafe clamp (m/s) — limited on the wheeled Go2-W
TELEOP_MAX_VYAW=0.6       # turn-rate clamp (rad/s)

# ── Remote periscope (directable HD video slice) ────────────────────────────
# See docs/periscope.md. Runs INSIDE the camera process (shares the live full-res
# frame), streams only the pointed-at slice on its own Zenoh session so it never
# starves pose. Client aims it with j/l (yaw) i/k (pitch) o/p (zoom) g (recenter)
# y (aspect) v (toggle panel) b (force keyframe).
PERISCOPE_ENABLE=1
# Resolution tier (short side px): 360 (low) | 480 (default) | 720 (high). The robot
# never upscales past the sensor — at 480 square, ~45deg FOV is 1:1 (see the doc).
PERISCOPE_RES=480
# Aspect ratio W:H — 1:1 (default, best for targeted inspection) | 4:3 | 16:9.
PERISCOPE_ASPECT=1:1
# FOV bounds (deg). Max capped 120-140 to avoid rectilinear edge blow-up (no fisheye).
PERISCOPE_MAX_FOV=130
PERISCOPE_MIN_FOV=20
PERISCOPE_DEFAULT_FOV=90
# Codec preference: h265 (HEVC, smaller) | h264 | mjpeg. Auto-falls back to H.264
# then per-frame MJPEG if PyAV/NVENC is unavailable (MJPEG works with zero extra deps).
# NOTE: the robot Docker image ships only opencv (no PyAV/NVENC), so h265/h264 there
# ALWAYS fall back to MJPEG anyway. Default to mjpeg to make that explicit and skip the
# per-rebuild 'encoder hevc_nvenc unavailable' fallback churn. The client viewer decodes
# MJPEG with cv2 (no PyAV needed). Switch back to h265 once PyAV+NVENC are in the robot
# image AND the client env has PyAV (client/pyproject.toml already pins 'av').
PERISCOPE_CODEC=mjpeg
PERISCOPE_JPEG_QUALITY=80
PERISCOPE_BITRATE=1500000
# Frame rate. Static PERISCOPE_FPS when dynamic is off; else ramp MIN..MAX (up while
# the operator is actively aiming, down when the view is static) to save uplink.
PERISCOPE_FPS=15
PERISCOPE_FPS_DYNAMIC=1
PERISCOPE_FPS_MIN=8
PERISCOPE_FPS_MAX=24
PERISCOPE_ACTIVE_WINDOW_S=1.5
# Extra HFOV (deg) rendered beyond the request so the client can micro-pan instantly.
PERISCOPE_OVERSCAN_DEG=10
# Keyframe (IDR) cadence (s) for drop recovery on the best-effort link.
PERISCOPE_IDR_INTERVAL_S=2.0
# Stop encoding this many seconds after the last view request (0 = always stream).
PERISCOPE_VIEWER_TIMEOUT_S=5.0
# Bounded send buffer (bytes) on the periscope uplink (bufferbloat guard). 0 = OS default.
PERISCOPE_SO_SNDBUF=262144

# ── Mapping server batching ─────────────────────────────────────────────────
# Process a PRISM window on (WINDOW_SIZE-OVERLAP) new frames OR this many seconds
# (watchdog), whichever first. 5 s suits the larger 16-frame window.
WINDOW_TIMEOUT_S=5.0
